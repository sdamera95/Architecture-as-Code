# Requirements Report — GroundRobotWithNav2

Auto-generated by the Architecture-as-Code bridge pipeline. Tier 1 design-time evaluation
performed by `syside.Compiler().evaluate(constraint, stdlib=..., experimental_quantities=True)`
against each requirement's owned `ConstraintUsage`. Generated at 2026-06-10T22:50:31+00:00.

**Summary:** 16 requirements —
5 satisfied,
0 violated,
11 indeterminate.

## Per-Requirement Status

| Status | Name | Kind | Doc | Constraints | Fatal |
|---|---|---|---|---|---|
| ✅ OK | `ShowcaseAGRNav2Full::MaxSpeedReq` | definition | Maximum speed on pedestrian paths: 1.5 m/s. | 1 | no |
| ✅ OK | `ShowcaseAGRNav2Full::OutdoorEnduranceReq` | definition | Minimum 6 hours outdoor operation on single charge. | 1 | no |
| ✅ OK | `ShowcaseAGRNav2Full::PayloadCapacityReq` | definition | Minimum 10 kg payload for package delivery. | 1 | no |
| ⚠️ ?? | `ShowcaseAGRNav2Full::GPSAccuracyReq` | definition | GPS-aided localization within 1 meter outdoors. | 0 | no |
| ✅ OK | `ShowcaseAGRNav2Full::PedestrianBrakingReq` | definition | Full stop within 0.3 seconds of pedestrian detection. | 1 | no |
| ⚠️ ?? | `ShowcaseAGRNav2Full::WeatherResistanceReq` | definition | IP67 rating for operation in rain and snow. | 0 | no |
| ✅ OK | `ShowcaseAGRNav2Full::GroundRobotWithNav2Requirements` | definition | System-level requirements group for the outdoor delivery robot. Binds the GroundRobotWithNav2 as subject; aggregates five requirements with per-requirement rationale preserved from the original satisfy docs. | 5 | no |
| ⚠️ ?? | `ShowcaseAGRNav2Full::DeliveryEfficiencyAnalysis::obj` | usage | Maximize deliveries per charge cycle by optimizing route selection and speed profiles. | 0 | no |
| ⚠️ ?? | `ShowcaseAGRNav2Full::GroundRobotWithNav2Requirements::maxSpeed` | usage | Nav2 ControllerServer max_vel parameter set to 1.5 m/s; VelocitySmoother enforces acceleration limits. | 0 | no |
| ⚠️ ?? | `ShowcaseAGRNav2Full::GroundRobotWithNav2Requirements::outdoorEndurance` | usage | 6S LiPo at 16 Ah with 40 W Orin + 19 W sensor load provides 7+ hours continuous outdoor operation. | 0 | no |
| ⚠️ ?? | `ShowcaseAGRNav2Full::GroundRobotWithNav2Requirements::payloadCapacity` | usage | Steel tube chassis rated for 33 kg total; with 18 kg robot mass, 15 kg payload capacity remains. | 0 | no |
| ⚠️ ?? | `ShowcaseAGRNav2Full::GroundRobotWithNav2Requirements::pedestrianBraking` | usage | CollisionMonitor triggers zero-velocity command within one control cycle (50 ms) upon pedestrian detection. | 0 | no |
| ⚠️ ?? | `ShowcaseAGRNav2Full::GroundRobotWithNav2Requirements::weatherResistance` | usage | WeatherproofEnclosure provides IP67 rating for all electronics; sensor units are individually sealed. | 0 | no |
| ⚠️ ?? | `ShowcaseAGRNav2Full::PedestrianBrakingReq::PedestrianSafety` | usage | _(no doc)_ | 0 | no |
| ⚠️ ?? | `ShowcaseAGRNav2Full::robotRqts` | usage | _(no doc)_ | 0 | no |
| ⚠️ ?? | `None` | usage | EKF fuses IMU at 100 Hz with LiDAR scan matching and AMCL pose, achieving sub-1m outdoor localization. | 0 | no |

## Interpretation Notes

- **OK** — every owned `ConstraintUsage` evaluated to `True` against the subject's default attribute values (for `requirement def`s) or the bound concrete values (for `requirement` usages).
- **FAIL** — at least one constraint evaluated to `False`. The model satisfies its structure but violates a stated budget or invariant.
- **??** — evaluation produced a non-Boolean value (likely a symbolic expression that the compiler could not reduce) OR the report's `fatal` flag was set. Either condition prevents a clear pass/fail call.
- **Indeterminate `??` is meaningful**: a requirement with `??` is not silently passing — it indicates that the model is missing the concrete values needed to make the constraint evaluable. The standard fix is either to (a) add concrete attribute values to the subject's `part def`, or (b) instantiate the requirement on a concrete `part` usage that supplies those values via `:>>` redefinition.

## Caveats

- This is **Tier 1 (design-time)** evaluation. Live runtime values from a ROS2 system are NOT injected — that would require Tier 2 (predicate compilation, "Approach A"), which is a separate framework feature.
- Per the Syside 0.9.0 docs example, a passing `requirement def` whose `assume constraint` failed will report as passed (vacuously satisfied — preconditions don't hold). If your requirements use `assume`, scrutinize `??` and `OK` cases together to distinguish "satisfied" from "preconditions failed."
