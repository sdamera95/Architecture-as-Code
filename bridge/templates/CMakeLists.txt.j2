# Auto-generated by the ros2-sysmlv2 bridge pipeline. Regenerated on every run.
cmake_minimum_required(VERSION 3.8)
project({{ pkg_name }})

if(CMAKE_COMPILER_IS_GNUCXX OR CMAKE_CXX_COMPILER_ID MATCHES "Clang")
  add_compile_options(-Wall -Wextra -Wpedantic)
endif()

find_package(ament_cmake REQUIRED)
find_package(rclcpp REQUIRED)
find_package(rclcpp_lifecycle REQUIRED)
{% for dep in custom_msg_deps %}
find_package({{ dep }} REQUIRED)
{% endfor %}

{% for exe in executables %}
add_executable({{ exe.executable }}
  src/{{ exe.module }}.cpp
  src/{{ exe.module }}_base.cpp)
target_include_directories({{ exe.executable }} PRIVATE include)
ament_target_dependencies({{ exe.executable }}
  rclcpp
  rclcpp_lifecycle
{% for dep in custom_msg_deps %}
  {{ dep }}
{% endfor %}
)

{% endfor %}
install(TARGETS
{% for exe in executables %}
  {{ exe.executable }}
{% endfor %}
  DESTINATION lib/${PROJECT_NAME})

# Language-neutral Python helpers (same scripts as the ament_python package).
install(PROGRAMS
  scripts/conformance_monitor
  scripts/activate_nodes
  DESTINATION lib/${PROJECT_NAME})

install(DIRECTORY launch config
  DESTINATION share/${PROJECT_NAME})

ament_package()
