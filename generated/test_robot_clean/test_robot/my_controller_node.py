#!/usr/bin/env python3
"""MyController — auto-generated from SysML v2 model.

Node: my_controller (/)
Archetype: Controller
Generated by: ros2-sysmlv2 bridge pipeline
IMPORTANT: Implement the callback stubs below. The publishers, subscribers,
action servers, and parameters are pre-wired from the architecture model.
"""
import rclpy
from rclpy.lifecycle import LifecycleNode, TransitionCallbackReturn, LifecycleState
from geometry_msgs.msg import Twist
from nav_msgs.msg import Odometry
from rclpy.qos import qos_profile_sensor_data, qos_profile_services_default


class MyController(LifecycleNode):
    """MyController lifecycle node.

    Instance name: my_controller
    Namespace: /
    """

    def __init__(self):
        super().__init__(node_name='my_controller')
        self.get_logger().info('MyController created (unconfigured)')

    def on_configure(self, state: LifecycleState) -> TransitionCallbackReturn:
        """Configure: create publishers, subscribers, and declare parameters."""
        self.get_logger().info('Configuring...')

        # Publisher: commandPub → /cmd_vel
        self.commandPub = self.create_lifecycle_publisher(
            Twist,
            '/cmd_vel',
            10)

        # Subscriber: stateSub ← /odom
        self.stateSub = self.create_subscription(
            Odometry,
            '/odom',
            self._stateSub_callback,
            10)

        # Parameter: max_velocity
        self.declare_parameter('max_velocity', 0.0)

        return TransitionCallbackReturn.SUCCESS

    def on_activate(self, state: LifecycleState) -> TransitionCallbackReturn:
        """Activate: start processing."""
        self.get_logger().info('Activating...')
        # TODO: Start timers, enable processing
        return super().on_activate(state)

    def on_deactivate(self, state: LifecycleState) -> TransitionCallbackReturn:
        """Deactivate: pause processing."""
        self.get_logger().info('Deactivating...')
        # TODO: Stop timers, disable processing
        return super().on_deactivate(state)

    def on_cleanup(self, state: LifecycleState) -> TransitionCallbackReturn:
        """Cleanup: release resources."""
        self.get_logger().info('Cleaning up...')
        return TransitionCallbackReturn.SUCCESS

    def on_shutdown(self, state: LifecycleState) -> TransitionCallbackReturn:
        """Shutdown: final cleanup."""
        self.get_logger().info('Shutting down...')
        return TransitionCallbackReturn.SUCCESS

    def _stateSub_callback(self, msg):
        """Callback for /odom (Odometry).

        TODO: Implement message processing logic.
        """
        pass


def main(args=None):
    rclpy.init(args=args)
    node = MyController()
    rclpy.spin(node)
    node.destroy_node()
    rclpy.shutdown()


if __name__ == '__main__':
    main()
