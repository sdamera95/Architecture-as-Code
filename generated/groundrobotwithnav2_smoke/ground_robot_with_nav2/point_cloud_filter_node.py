#!/usr/bin/env python3
"""PointCloudFilter — auto-generated from SysML v2 model.

Node: pointcloud_filter (/perception)
Archetype: PerceptionPipeline
Generated by: ros2-sysmlv2 bridge pipeline
Mode: SMOKE TEST — publishers emit default messages for topology visualization.
      Remove the smoke timers and implement real callback logic before deployment.
"""
import rclpy
from rclpy.lifecycle import LifecycleNode, TransitionCallbackReturn, LifecycleState
from sensor_msgs.msg import Image
from sensor_msgs.msg import PointCloud2


class PointCloudFilter(LifecycleNode):
    """PointCloudFilter lifecycle node.

    Instance name: pointcloud_filter
    Namespace: /perception
    """

    def __init__(self):
        super().__init__(node_name='pointcloud_filter')
        self.get_logger().info('PointCloudFilter created (unconfigured)')

    def on_configure(self, state: LifecycleState) -> TransitionCallbackReturn:
        """Configure: create publishers, subscribers, and declare parameters."""
        self.get_logger().info('Configuring...')

        # Publisher: processedPub → /filtered_cloud
        self.processedPub = self.create_lifecycle_publisher(
            PointCloud2,
            '/filtered_cloud',
            10)

        # Subscriber: rawSub ← /depth/image
        self.rawSub = self.create_subscription(
            Image,
            '/depth/image',
            self._rawSub_callback,
            10)

        # Parameter: voxel_size
        self.declare_parameter('voxel_size', 0.0)

        return TransitionCallbackReturn.SUCCESS

    def on_activate(self, state: LifecycleState) -> TransitionCallbackReturn:
        """Activate: start processing."""
        self.get_logger().info('Activating...')

        # ── Smoke test: publish default messages for topology visualization ──
        self._processedPub_timer = self.create_timer(
            1.0, self._processedPub_smoke_publish)
        return super().on_activate(state)

    def on_deactivate(self, state: LifecycleState) -> TransitionCallbackReturn:
        """Deactivate: pause processing."""
        self.get_logger().info('Deactivating...')
        self._processedPub_timer.cancel()
        return super().on_deactivate(state)

    def on_cleanup(self, state: LifecycleState) -> TransitionCallbackReturn:
        """Cleanup: release resources."""
        self.get_logger().info('Cleaning up...')
        return TransitionCallbackReturn.SUCCESS

    def on_shutdown(self, state: LifecycleState) -> TransitionCallbackReturn:
        """Shutdown: final cleanup."""
        self.get_logger().info('Shutting down...')
        return TransitionCallbackReturn.SUCCESS

    def _rawSub_callback(self, msg):
        """Callback for /depth/image — smoke mode: log receipt."""
        self.get_logger().debug(f'[smoke] Received on /depth/image')

    def _processedPub_smoke_publish(self):
        """Smoke publisher for /filtered_cloud — emits default PointCloud2."""
        msg = PointCloud2()
        self.processedPub.publish(msg)


def main(args=None):
    rclpy.init(args=args)
    node = PointCloudFilter()
    rclpy.spin(node)
    node.destroy_node()
    rclpy.shutdown()


if __name__ == '__main__':
    main()
